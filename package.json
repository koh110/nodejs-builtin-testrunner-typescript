{
  "private": true,
  "type": "module",
  "name": "nodejs-builtin-testrunner-typescript",
  "scripts": {
    "build": "tsc",
    "prestart": "npm run build",
    "start": "node dist/index.js",
    "test": "node --experimental-strip-types --test src/*.test.ts src/**/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^22.5.5",
    "typescript": "^5.6.2"
  }
}
